meta {
  name: Retrieve tag
  type: http
  seq: 2
}

get {
  url: http://localhost:5000/urls?tags={{tag}}
  body: none
  auth: inherit
}

params:query {
  tags: {{tag}}
}

vars:pre-request {
  tag: both
}
